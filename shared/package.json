{
  "name": "@blog-platform/shared",
  "version": "1.0.0",
  "description": "Shared types and constants for the AI-powered blog platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.2",
    "rimraf": "^5.0.5"
  },
  "dependencies": {},
  "files": [
    "dist"
  ]
}